bool pathFinder(Creep);


bool pathFinder(Creep creep){
    for (creep.current[x][y]-1
        if(obstacle==true)
            return false;
            else
            return true;

            creep.current[x][y]+1
                if(obstacle==true)
                    return false;
                else
                    return true;

            creep.current[x]+1[y]
                 if(obstacle==true)
                    return false;
                 else
                    return true;

}


